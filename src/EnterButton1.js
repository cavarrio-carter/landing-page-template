import Button from '@mui/material/Button';
import Stack from '@mui/material/Stack'

/*add anchor & URL to redirect to hosted project: temp; ultimately
add to portfolio project and use React to route pages within same project
for faster, client-side loading */
export default function EnterButton1 () {
  return (
      <Button variant="outlined">Enter</Button>
  );
}
